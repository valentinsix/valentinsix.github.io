<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://valentinsix.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://valentinsix.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-06T14:20:12+00:00</updated><id>https://valentinsix.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">A Gentle Introduction to Mechanistic Interpretability</title><link href="https://valentinsix.github.io/blog/2026/mechinterp/" rel="alternate" type="text/html" title="A Gentle Introduction to Mechanistic Interpretability"/><published>2026-07-03T00:00:00+00:00</published><updated>2026-07-03T00:00:00+00:00</updated><id>https://valentinsix.github.io/blog/2026/mechinterp</id><content type="html" xml:base="https://valentinsix.github.io/blog/2026/mechinterp/"><![CDATA[<p>I sometimes find myself explaining mechanistic interpretability to people from scratch, so I figured I’d write the explanation down once, mostly to avoid vague explanations. If you’ve trained a few neural networks, you already know the uncomfortable truth about deep learning: we’re very good at getting these models to work, and not very good at explaining <em>why</em> they work. You can train a transformer to write code, translate languages, or beat you at chess, but ask “how exactly is it doing that?” and the honest answer, for most of deep learning’s history, has been a shrug and a matrix of a billion floating point numbers.</p> <p><strong>Mechanistic interpretability</strong> (often shortened to “mech interp” or “MI”) is a research field trying to change that. The goal isn’t just to say “this input leads to that output.” It’s to reverse-engineer the algorithm a trained network has learned, in enough detail that you could, in principle, rewrite it as normal code. This post is a friendly introduction with enough background to understand the vocabulary, the main ideas, and why people find this exciting (and difficult).</p> <p>I’ll assume you know the basics of neural networks and a bit of linear algebra, but nothing about interpretability specifically.</p> <hr/> <h2 id="why-bother-looking-inside-at-all">Why bother looking inside at all?</h2> <p>There are a few different motivations that draw people into this field, and it’s worth separating them because they lead to different research styles.</p> <p><strong>Safety:</strong> as models get more capable, we’d like some guarantee that they’re doing what we think they’re doing, for the reasons we think they’re doing it. A model that’s honest by coincidence looks identical from the outside to a model that’s honest because it has learned to appear that way when watched. If we can’t look inside, we can’t tell these apart. Interpretability is one of the few tools that could, in principle, distinguish “actually aligned” from “successfully faking it.”</p> <p><strong>Scientific curiosity:</strong> separately from safety, this is just a fascinating scientific question. Deep learning produces systems that do impressive things, and we mostly don’t know how. That’s an intellectually strange position to be in as a field: it would be like chemistry with no periodic table, just “put things together, see what happens.” Mechanistic interpretability is an attempt to build a periodic table for what’s going on inside trained networks.</p> <p><strong>Improving the models:</strong> more practically, understanding internal mechanisms helps you fix models. If you know <em>why</em> a model hallucinates, or <em>why</em> it fails on a certain class of inputs, you can fix the actual problem instead of patching the symptom with more training data.</p> <p>All three motivations point to the same technical project: open up the black box and find the algorithm inside. The scientific angle is what pulled me into the field, but the safety aspect is a concrete and industry-related one, which makes the problem interesting AND important.</p> <hr/> <h2 id="the-core-idea-networks-as-programs">The core idea: networks as programs</h2> <p>The central move in mechanistic interpretability is a shift in mindset. Instead of thinking of a neural network as <em>a function that was fit to data</em>, you think of it as <em>a program that was compiled by gradient descent</em>. Somewhere inside those weights is a set of subroutines: computational structures that implement specific behaviors. And our job is of course to find them.</p> <p>But this might sound like a strange thing to hope for: why would gradient descent even produce anything as clean as a “subroutine”? The empirical answer, from several years of work by researchers (Anthropic, Redwood Research, and various other academic labs) is: it does, and more often than you’d really expect. Networks seem to learn genuinely modular, reusable pieces of computation, and when you find them, they sometimes make intuitive sense.</p> <p>The two central objects in this program-reconstruction project are <strong>features</strong> and <strong>circuits</strong>.</p> <hr/> <h2 id="features-aka-the-variables-of-the-program">Features, a.k.a. the “variables” of the program</h2> <p>A <strong>feature</strong> is (informally) a property of the input that the network represents internally: something that’s “on” for some inputs and “off” (or scaled) for others. Classic examples from vision models: a “curve detector” neuron, a “dog ear” detector, a “car wheel” detector. In language models: a feature that fires on legal text, a feature that tracks whether we’re inside quotation marks, a feature that fires on grammatical subjects…</p> <p>If neurons neatly corresponded to features (one neuron = one concept), mechanistic interpretability would be almost too easy: you’d just look at what makes each neuron fire, label it, and move on. Early interpretability work on vision networks found many neurons like this. But as people looked more carefully, especially in language models, this clean picture broke down.</p> <hr/> <h2 id="superposition-too-many-concepts-not-enough-neurons">Superposition: too many concepts, not enough neurons</h2> <p>If you look at an individual neuron in a language model, it often seems to respond to a weird, unrelated basket of things: say, it fires on French text, on mentions of the number seven, and on requests for recipes. This is called <strong>polysemanticity</strong>: one neuron = many unrelated meanings.</p> <p>Why would a network do this? The leading explanation is a phenomenon called <strong>superposition</strong>. The idea, roughly:</p> <ul> <li>The world has <em>far more</em> meaningful features in it than any network has neurons available to represent them.</li> <li>Real-world features are usually <strong>sparse</strong>: most concepts are absent most of the time (a given sentence usually isn’t about French, the number seven, and recipes all at once; actually most of the time none of these are active).</li> <li>Because most features are sparse, a network can get away with squeezing <em>many more features than it has dimensions</em> into its representation space, as long as those features rarely need to be active simultaneously. When they do overlap, you get a bit of interference (noise), but if it’s rare enough, it’s a price worth paying compared to only representing as many concepts as you have neurons.</li> </ul> <p>Analogy time: imagine coat-check system where you’re allowed to hang multiple coats on one hook, because you know it’s unlikely that all the coats sharing a hook get claimed on the same day. Sometimes two people show up for the same hook and there’s a mix-up. But you can serve far more customers this way than if you insisted on one coat per hook.</p> <p>To be slightly more formal, lhe <strong>linear representation hypothesis</strong> (a working assumption behind most of this research) says that a layer’s activation vector $x \in \mathbb{R}^d$ can be written as a sparse combination of feature directions:</p> \[x = \sum_{i=1}^{n} f_i \, v_i\] <p>where each $v_i \in \mathbb{R}^d$ is a fixed direction representing “feature $i$,” each $f_i \geq 0$ tells you how strongly that feature is active for this particular input, and only a small number of the $f_i$ are non-zero at once (hence the sparsity).</p> <p>A key detail is that the number of features $n$ can be (and often is) <strong>much larger than the number of dimensions $d$</strong> available to represent them ($n \gg d$). That’s only possible geometrically because the $v_i$ are not orthogonal: they overlap a bit, so activating one feature leaks a small amount into the directions of others. As long as features are rarely on at the same time, that leakage stays small and the network can still mostly recover which features were active.</p> <p>Superposition means that individual neurons are generally <strong>not</strong> the right unit of analysis. Real features live along <em>directions</em> in the high-dimensional activation space, and many of them don’t line up neatly with any single neuron’s axis. This is one of the main reasons mechanistic interpretability is hard: the thing you actually want to look at (a feature) doesn’t correspond to something you can directly read off (a neuron).</p> <h3 id="unmixing-features-with-sparse-autoencoders">Unmixing features with Sparse Autoencoders</h3> <p>If features are directions in activation space rather than individual neurons, we need a way to find those directions. The current favorite tool is the <strong>sparse autoencoder (SAE)</strong>.</p> <p>The basic idea is to train a small auxiliary network that takes a layer’s activations, projects them into a much <em>higher-dimensional</em> space (more “slots” than the original number of neurons), and reconstructs the original activations, while forcing most of the high-dimensional slots to be zero (inactive) for any given input. If this works, each of those slots tends to correspond to something close to a genuine, monosemantic feature (a single interpretable concept) rather than a polysemantic neuron.</p> <p>Concretely, an SAE learns an encoder and decoder:</p> \[z = \text{ReLU}(W_{\text{enc}} \, x + b_{\text{enc}}), \qquad \hat{x} = W_{\text{dec}} \, z + b_{\text{dec}}\] <p>where $z \in \mathbb{R}^m$ with $m \gg d$ (many more slots than the original layer had neurons), and it’s trained to minimize a loss that trades off reconstruction accuracy against sparsity of $z$:</p> \[\mathcal{L} = \underbrace{\|x - \hat{x}\|_2^2}_{\text{reconstruction}} + \lambda \underbrace{\|z\|_1}_{\text{sparsity penalty}}\] <p>The $\ell_1$ penalty pushes most entries of $z$ to exactly zero for any given input, which is what encourages each surviving non-zero entry to correspond to a single, clean, interpretable feature rather than a blend of several. This has been one of a very productive development in the field, as it gives you a somewhat automated way of pulling human-interpretable concepts out of superposed representations, at least approximately.</p> <figure> <picture> <img src="/assets/img/mechinterp/sae-diagram.png" class="img-fluid rounded z-depth-1 mx-auto d-block diagram-on-light" width="100%" height="auto" style=" max-width: 480px; " data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">A sparse autoencoder reconstructing a target network&#39;s activations through a wider, sparsely-active hidden layer, in the hope that each active unit lines up with a single genuine feature. <a href="https://www.alignmentforum.org/posts/tLCBJn3NcSNzi5xng/deep-sparse-autoencoders-yield-interpretable-features-too" target="_blank">Source</a>.</figcaption> </figure> <hr/> <h2 id="circuits-features-wired-together">Circuits: features wired together</h2> <p>Finding individual features is cool, but not nearly enough to do something useful with it. A program isn’t just a list of variables, they also need to be <em>connected by computation</em>. In mech interp, the analogous idea is a <strong>circuit</strong>: a subgraph of the network (a specific set of features, or neurons/attention heads, and the connections between them) that together implement some identifiable piece of behavior.</p> <p>For example: a circuit might be the specific combination of attention heads and MLP neurons that lets a language model figure out that in the sentence <em>“When Mary and John went to the store, John gave a drink to ___“</em>, the blank should be “Mary” rather than “John.” That’s a real, well-studied example (the “Indirect Object Identification” circuit, more on it below). Researchers have traced out, head by head, how the model tracks who has already been mentioned, who the subject is, and who the object should be.</p> <p>Once you have both features and circuits, you have the two ingredients of the reverse-engineering project: <em>what</em> the network represents, and <em>how</em> those representations get combined to produce outputs.</p> <hr/> <h2 id="the-toolbox">The toolbox</h2> <p>There are standard techniques that come up repeatedly. It’s maybe worth knowing their names, since they show up in most papers in the domain.</p> <p><strong>Probing:</strong> train a small, simple classifier (like linear regression) on a model’s internal activations to see if some property of interest (e.g., “is this token inside a negation?”) is linearly recoverable from them. If a simple probe can extract it easily, the model likely represents that property in a fairly clean, accessible way. Probing tells you <em>that</em> information is present, but not that the model actually <em>uses</em> it. For that, you need causal methods.</p> <p><strong>Ablation:</strong> zero out (or otherwise remove) a component (a neuron, an attention head, a whole layer) and see what breaks. If deleting a specific attention head destroys the model’s ability to do subject-verb agreement but doesn’t touch anything else, that’s evidence the head is doing something specific and identifiable.</p> <p><strong>Activation patching / causal tracing:</strong> you run the model on two versions of an input that differ in one key way: a “clean” run and a “corrupted” run (e.g., “The Eiffel Tower is in Paris” vs. “The Eiffel Tower is in Rome”). Then, for some component $h$ (a specific neuron, head, or layer) you copy its activation from the clean run into the corrupted run, and measure how much this shifts the output metric $M$ (e.g., the model’s probability on the correct answer) back toward the clean result:</p> \[\text{Effect}(h) = M\big(\text{corrupted}_{\,h \leftarrow \text{clean}}\big) - M(\text{corrupted})\] <p>A large effect means that component is causally responsible for carrying the relevant information. Doing this systematically across every component gives you a map of <em>where</em> in the network a given piece of information lives and gets used. That’s how circuits like IOI (below) get traced out.</p> <p><strong>Attribution methods:</strong> techniques (often gradient-based) that estimate how much each internal component contributed to the final output, without needing to run the full model twice per component like ablation does. Faster, but generally a rougher approximation than direct ablation or patching.</p> <p><strong>Sparse autoencoders</strong>, as covered above, for unmixing superposed features into interpretable directions.</p> <p>More generally, the field has moved from “purely observational” methods (probing: what’s correlated with what) toward “causal” methods (patching, ablation: what actually matters for the output). Correlation-only findings can be misleading about what the model is actually doing versus what’s just incidentally present. A personal comment on the current state of MI: it seems to me that most efforts in the field converge on analzying the model AFTER it has been trained. But I haven’t found a lot of work focusing on <em>how the model in gradually learning</em>, and how we arrive to the final model. <a href="https://valentinsix.github.io/blog/2026/slt/">Developmental Interpretability</a> is a young field that seems to focus on this gap. Or maybe this kind of work is mostly about training dynamics, and not so much “proper interpretability”. If you have an opinion about this, please reach out!</p> <hr/> <h2 id="a-few-concrete-case-studies">A few concrete case studies</h2> <p>Abstract descriptions only get you so far, and it is actually one of the strong criticism the filed is facing. But here are some of the field’s most-cited concrete results:</p> <p><strong>Induction heads:</strong> it’s one of the earliest and cleanest findings. Many transformers develop a specific type of attention head, called an induction head, that implements a simple pattern-completion rule. If token $A$ was followed by token $B$ earlier in the context, and $A$ shows up again, the head boosts the prediction for $B$:</p> \[\text{if } \ldots, A, B, \ldots, A, \_\_ \quad \Rightarrow \quad P(\text{next} = B) \uparrow\] <p>This is a big part of how models do in-context learning: copying and completing patterns they’ve seen earlier in their own context window. Induction heads also tend to emerge somewhat suddenly during training, and that emergence tracks with a jump in the model’s in-context learning ability, a nice concrete link between “a specific mechanism appearing” and “a capability appearing.”</p> <p><strong>The Indirect Object Identification (IOI) circuit:</strong> given a sentence like <em>“When Mary and John went to the store, John gave a drink to ___,”</em> GPT-2 small reliably predicts “Mary.” Researchers traced out the full circuit responsible: specific attention heads that identify duplicate names, other heads that figure out who the subject of the sentence is, and heads that move the correct name to the output. This was one of the first times a non-trivial language model behavior was mapped out almost completely, head by head.</p> <p><strong>Modular addition and grokking:</strong> A small transformer trained to compute addition modulo some prime number will, after a long plateau, suddenly “grok” the task: it goes from from memorizing the training examples to generalizing perfectly. When researchers looked inside a grokked network, they found it had learned to represent each number $a$ as a point on a circle, roughly as $(\cos(\omega a), \sin(\omega a))$ for some learned frequency $\omega$. Addition then falls out almost for free from the angle-addition identity:</p> \[\cos(\omega(a+b)) = \cos(\omega a)\cos(\omega b) - \sin(\omega a)\sin(\omega b)\] <p>The network’s internal multiplications and additions turn out to implement essentially this trigonometric identity, which is why the result generalizes perfectly instead of just memorizing training examples: it’s a genuine, compact arithmetic algorithm.</p> <figure> <picture> <img src="/assets/img/mechinterp/grokking-1-layer-transformer.webp" class="img-fluid rounded z-depth-1 diagram-on-light" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">The grokked one-layer transformer&#39;s internal circuit for modular addition: numbers get embedded onto a circle, combined via trig identities, and read off into logits. <a href="https://www.neelnanda.io/grokking-paper" target="_blank">Source</a>.</figcaption> </figure> <p>These examples matter beyond their specific content: they’re existence proofs. They show that at least in some cases, real trained networks <em>do</em> implement clean, human-legible algorithms. And that’s the basic bet the whole field is making.</p> <hr/> <h2 id="why-this-is-hard">Why this is hard</h2> <p>It’s worth being honest about the obstacles, since they explain why “understand GPT-5.5 completely” isn’t around the corner.</p> <p><strong>Superposition, again:</strong> as mentioned earlier, most interesting representations aren’t cleanly aligned with individual neurons, so almost all analysis needs an “unmixing” step first, and that step is itself imperfect and an active research problem.</p> <p><strong>Scale:</strong> the case studies above are mostly on small models (GPT-2 scale or smaller) and narrow behaviors (a specific arithmetic task, a specific sentence pattern). Frontier models have orders of magnitude more parameters and are used for wildly more diverse tasks. Circuit-level understanding doesn’t obviously scale: we don’t yet have a clear plan for going from “we understand this one circuit in a small model” to “we understand a large fraction of a frontier model’s behavior.” It’s also easiest to study things that are easy to study, which are often well-defined, narrow, static behaviors on small models. There’s a real risk of the field over-indexing on convenient examples rather than the messy, high-stakes behaviors we most want to understand (deception, situational awareness, goal-directed planning…).</p> <p><strong>No ground truth:</strong> unlike, say, biology, where you can eventually verify a hypothesis about a mechanism by direct experiment, it’s often unclear how to be fully certain an interpretability explanation is <em>correct</em> rather than just <em>plausible-sounding</em>. This is honestly the part that frustrates me most. A lot of SAE features get called “interpretable” because a researcher scanned the examples that activate it and the label felt right, not because anyone checked that the feature actually does what the label claims. That’s manual, eyeball-it, and it’s the kind of manual work that doesn’t obviously get more reliable just because you do more of it. Maybe I’m being too harsh here, plenty of people in the field take this seriously and are actively trying to build better validation methods, but until there’s a real way to check a feature’s claimed meaning against something other than “yeah, that tracks,” I stay somewhat skeptical of how much SAEs have actually solved.</p> <hr/> <h2 id="where-the-field-is-headed">Where the field is headed</h2> <p>A few current directions are worth knowing about. One is <strong>scaling sparse autoencoders</strong> to frontier-size models, to see whether the “unmixing into clean features” approach holds up outside of toy settings. Another is <strong>automating interpretability</strong>: using language models themselves to generate and test hypotheses about what other models’ components are doing, since manual circuit-tracing doesn’t scale to human labor alone. A third, newer offshoot is <strong>developmental interpretability</strong> (mentionned ealier ; see linked post), which looks at <em>how</em> circuits and features emerge over the course of training, rather than only analyzing a finished model. And there’s growing interest in <strong>interpretability for alignment evaluation</strong>: using internal probes as a kind of “lie detector,” to try to catch cases where a model’s stated reasoning doesn’t match what it’s actually doing internally.</p> <hr/> <h2 id="wrapping-up-and-how-to-start">Wrapping up and how to start</h2> <p>A few concrete starting points: Anthropic’s <strong>Transformer Circuits</strong> publications are the closest thing the field has to a canonical reading list, starting with the original circuits work on vision models and moving through induction heads, superposition, and sparse autoencoders. <strong>TransformerLens</strong> is an open-source library built specifically for this kind of research: it makes it easy to grab internal activations, run activation patching experiments, and poke around small models like GPT-2. Replicating a small, well-documented result yourself is a much better way to build intuition than reading about it.</p> <p>Mechanistic interpretability is, at its core, a huge bet: that the strange, huge-dimensional, seemingly opaque computations inside a trained neural network aren’t actually an unstructured mess, but a genuine algorithm, messy in places, but ultimately built out of identifiable and reusable parts. The field has real, concrete wins to point to, and honest obstacles still in front of it. I don’t think anyone in the field would tell you it’s close to “solved”.</p> <hr/> <p><em>If you spot something inaccurate here, feel free to reach out. This is very much a “learning in public” post.</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Laying down the basics and justifying the existence of the field.]]></summary></entry><entry><title type="html">Singular Learning Theory and Developmental Interpretability</title><link href="https://valentinsix.github.io/blog/2026/slt/" rel="alternate" type="text/html" title="Singular Learning Theory and Developmental Interpretability"/><published>2026-07-02T00:00:00+00:00</published><updated>2026-07-02T00:00:00+00:00</updated><id>https://valentinsix.github.io/blog/2026/slt</id><content type="html" xml:base="https://valentinsix.github.io/blog/2026/slt/"><![CDATA[<p>I’ve been reading about Singular Learning Theory (SLT) lately, a framework developed by the statistician Sumio Watanabe, and I wanted to write down what I’ve understood so far: partly to organize my own thoughts, partly because it’s one of the more interesting ideas floating around in the “why does deep learning even work” corner of ML theory. This post is a friendly overview, not a technical deep dive. If you’re starting a PhD and keep hearing “SLT” and “developmental interpretability” thrown around, hopefully this helps.</p> <h2 id="the-problem-with-classical-learning-theory">The problem with classical learning theory</h2> <p>Classical statistical learning theory makes a convenient assumption: models are <em>regular</em>. In plain terms, this means every distinct setting of the parameters gives a distinct probability distribution, and the loss landscape around the best parameters looks roughly like a smooth bowl, essentially a Gaussian. This is a nice assumption because it lets you do a lot of clean math around it.</p> <p>Concretely, if $w_0$ is the true (best) parameter and $K(w)$ is the population loss (the KL divergence between the true distribution and the model’s distribution),</p> \[K(w) = \mathbb{E}_{x}\left[\log \frac{q(x)}{p(x \mid w)}\right],\] <p>then the regularity assumption says $K(w)$ is well approximated, near $w_0$, by a simple quadratic bowl:</p> \[K(w) \approx \frac{1}{2}(w - w_0)^\top I(w_0)(w - w_0),\] <p>where $I(w_0)$ is the Fisher Information Matrix (defined below) evaluated at $w_0$. This is exactly the Taylor expansion of a smooth, bowl-shaped function around its minimum, and it’s only valid if $I(w_0)$ is invertible. Invertibility is what makes the bowl curve upward in every direction: if $I(w_0)$ had a zero eigenvalue, the quadratic term would vanish along that direction, and $K(w)$ would stay flat there instead of curving up. It also underlies classical asymptotic theory more broadly, since the standard result that the maximum likelihood estimator is asymptotically normal, with covariance $I(w_0)^{-1}$, requires that inverse to exist.</p> <p>The problem is that this assumption is basically false for almost every model we actually care about: neural networks, hidden Markov models, and so on. These models are heavily <strong>overparameterized</strong>: many different parameter settings can produce the exact same input-output behavior. The “bowl” assumption breaks down.</p> <h2 id="what-makes-a-model-singular">What makes a model singular</h2> <p>This is where Singular Learning Theory comes in. SLT studies exactly the case that classical theory ignores: models that have <strong>singularities</strong>, which are points in parameter space where the Fisher Information Matrix is degenerate.</p> <p>A quick refresher on the Fisher Information Matrix (FIM): it measures how <em>sensitive</em> a model is to small changes in its parameters. If you nudge a parameter slightly, how much does the model’s output change? Formally,</p> \[I(w) = \mathbb{E}_{x \sim p(x \mid w)}\Big[\nabla_w \log p(x \mid w)\, \nabla_w \log p(x \mid w)^\top\Big],\] <p>i.e. the covariance of the score function (the gradient of the log-likelihood).</p> <p>The FIM is degenerate, or non-invertible, when</p> \[\det I(w_0) = 0.\] <p>That means the matrix applies a transformation that can’t be undone: some information gets collapsed. Concretely, there exists some direction $v \neq 0$ with $v^\top I(w_0) v = 0$, meaning $K(w_0 + \epsilon v) \approx 0$ to second order. Practically, this shows up as <strong>singularities</strong>: parameter directions that don’t actually matter. In other words, you can move the parameters in these directions without changing the model’s behavior at all. These are the redundant, “invisible” directions of the model.</p> <figure> <picture> <img src="/assets/img/slt/havz3x4miy2yngbbqzyl.webp" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">A sharp, bowl-shaped minimum (regular case, left) next to a flat, wide minimum (singular case, right). Optimization trajectories (red) settle much faster in the sharp bowl than in the flat one. <a href="https://www.lesswrong.com/posts/fovfuFdpuEwQzJu2w/neural-networks-generalize-because-of-this-one-weird-trick" target="_blank">Source</a>.</figcaption> </figure> <h2 id="measuring-complexity-the-rlct">Measuring complexity: the RLCT</h2> <p>If some parameters are redundant, then just counting the number of parameters is a bad way to measure a model’s real complexity. SLT instead uses a quantity called the <strong>Real Log Canonical Threshold (RLCT)</strong>, usually written as λ. The RLCT captures the model’s <em>true effective complexity</em>: how much of the parameter space actually matters, geometrically, around a given point.</p> <p>One intuitive way to define it: look at how the volume of “near-optimal” parameters shrinks as you tighten the loss threshold $\varepsilon$ around $w_0$,</p> \[\mathrm{Vol}\big(\{w : K(w) \le \varepsilon\}\big) \sim C\, \varepsilon^{\lambda} \quad \text{as } \varepsilon \to 0.\] <p>In the regular case, $K(w)$ is a $d$-dimensional quadratic bowl, so each of its $d$ axes shrinks like $\sqrt{\varepsilon}$ as you tighten the threshold, and the volume of that ellipsoid scales as the product of its axis lengths, $(\sqrt{\varepsilon})^d = \varepsilon^{d/2}$. That’s exactly why classical theory predicts $\lambda = d/2$, half the parameter count. In the singular case, the flat directions make the near-optimal region shrink <em>more slowly</em> as $\varepsilon \to 0$, so $\lambda &lt; d/2$: a direct, geometric sense in which the model is “simpler” than its raw parameter count suggests.</p> <p>This shows up in a quantity called the <strong>free energy</strong>, which we want to minimize. Intuitively, the free energy is the (negative log) Bayesian evidence for a region of parameter space: how strongly the data supports that region once you’ve paid a fair price for how much of the hypothesis space it occupies. A region with low free energy is one the data prefers, either because it fits well, because it’s geometrically simple, or some balance of both. Watanabe’s asymptotic formula for the (negative log) marginal likelihood over $n$ samples is:</p> \[F_n = n K_n(w_0) + \lambda \log n - (m - 1) \log \log n + O(1),\] <p>where $m$ is a multiplicity term (usually $1$) and $K_n(w_0)$ is the empirical loss at the optimum. Dropping the lower-order terms gives the simplified picture I’ll use for intuition:</p> \[\text{Free energy} \approx \underbrace{\text{fit to data}}_{\text{loss}} + \lambda \cdot \log(n)\] <p>where:</p> <ul> <li>the <strong>fit to data</strong> term is just the loss (smaller means the model fits the data better),</li> <li><strong>λ (RLCT)</strong> is the effective complexity,</li> <li><strong>n</strong> is the sample size.</li> </ul> <p>The intuition is nice once you see it. When λ is low, the loss landscape is flat around this region, so the effective complexity is low: a <strong>flat singularity</strong>. When λ is high, the landscape is sharp, so the effective complexity is high: a <strong>sharp singularity</strong>.</p> <p>A flat region means there are directions around this optimum that don’t really matter, so the model, in some sense, is “simpler” than it looks, even if it has millions of parameters. Free energy formalizes the classic intuition that flatter minima tend to generalize better.</p> <h2 id="why-bother-with-slt">Why bother with SLT?</h2> <p>The main motivation is that SLT gives us a language to talk about strange deep learning phenomena that classical theory can’t explain: things like <strong>phase transitions</strong>, <strong>grokking</strong>, and <strong>double descent</strong>. These are all cases where a model’s behavior changes abruptly or non-monotonically during training, which doesn’t fit neatly into the “smooth bowl” picture at all.</p> <p>That said, I should be honest: SLT is still not very practical, at least not yet. λ can be computed exactly for small toy models, but it’s intractable for anything the size of a real neural network. It’s linked to generalization performance, which is promising, but that connection isn’t fully worked out. Most of the vocabulary and theorems were built for Bayesian models, and extending them to networks trained with SGD is promising but still not rigorous. So far, SLT hasn’t produced concrete training improvements or architectural insights: it’s more of a theoretical lens than an engineering toolkit.</p> <h2 id="the-link-with-developmental-interpretability">The link with developmental interpretability</h2> <p>This is the part I find most exciting. There’s a growing research direction called <strong>developmental interpretability (DI)</strong> that uses SLT as a lens for mechanistic interpretability (MI), but with a twist. Instead of just analyzing the <em>final</em> trained model, DI looks at how a neural network changes <em>during</em> training.</p> <p>The name is a deliberate echo of developmental biology, the study of how a single fertilized cell turns into a fully formed organism through a sequence of developmental stages, rather than just how the final adult body is structured. DI borrows that framing: instead of asking what a trained network computes, it asks how the network’s internal structure unfolds over the course of training, one stage at a time.</p> <p>Here’s the connection to phase transitions: SGD can be thought of as moving between different singular regions of the loss landscape over the course of training. Each region roughly corresponds to a different “computational strategy” the network is using internally. A phase transition, then, is a sudden shift from one region (one strategy) to another.</p> <p>This is a genuinely different question from what most MI work asks. Most MI research starts from a <em>finished</em> network and tries to reverse-engineer what it’s doing. Developmental interpretability instead asks: <strong>how does the network become what it is?</strong> It’s less about dissecting the final artifact and more about understanding the process that produced it.</p> <figure> <picture> <img src="/assets/img/slt/obtwqsdl4yi3ch5zvk6k.webp" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">As training progresses, the network&#39;s parameters move across the singular loss landscape, crossing from one singular region into another: a phase transition. <a href="https://www.lesswrong.com/posts/TjaeCWvLZtEDAS5Ex/towards-developmental-interpretability" target="_blank">Source</a>.</figcaption> </figure> <h2 id="where-things-stand">Where things stand</h2> <p>Combining SLT and developmental interpretability is still a very young idea, more of a research program than an established field. There’s a real theoretical foundation (SLT has been around in the statistics literature for a while), but connecting it usefully to how we actually train and interpret large neural networks is very much a work in progress.</p> <p>That’s part of what makes it appealing to me: the foundations are solid, but there’s clearly a lot left to figure out.</p> <h2 id="additional-sources">Additional sources</h2> <ul> <li><a href="https://www.lesswrong.com/posts/TjaeCWvLZtEDAS5Ex/towards-developmental-interpretability">Towards Developmental Interpretability</a></li> <li><a href="https://www.lesswrong.com/posts/fovfuFdpuEwQzJu2w/neural-networks-generalize-because-of-this-one-weird-trick">Neural networks generalize because of this one weird trick</a></li> <li><a href="https://www.youtube.com/watch?v=cM2IRY0I4J8&amp;t=543s">Jesse Hoogland - Singular Learning Theory and AI Safety</a></li> <li><a href="https://www.youtube.com/watch?v=H5VJuKsmtZ8">Theoretical And Empirical Aspects Of Singular Learning Theory For AI Alignment</a></li> </ul> <hr/> <p><em>If you spot something inaccurate here, feel free to reach out. This is very much a “learning in public” post.</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Explaining my basic understanding of SLT and how we can use it for interpretability.]]></summary></entry><entry><title type="html">Why Writing a Blog?</title><link href="https://valentinsix.github.io/blog/2025/blog/" rel="alternate" type="text/html" title="Why Writing a Blog?"/><published>2025-11-26T00:00:00+00:00</published><updated>2025-11-26T00:00:00+00:00</updated><id>https://valentinsix.github.io/blog/2025/blog</id><content type="html" xml:base="https://valentinsix.github.io/blog/2025/blog/"><![CDATA[<p>Welcome to my own Latent Thoughts!</p> <p>I never found a convincing reason to start a blog, but now I want a place to put down ideas as they come, without worrying about growing an audience or building a brand around it.</p> <p>This is, first and foremost, a collection of ideas, thoughts, notes, and opinions. I share it here to centralize those ideas in one place and make them available to everyone, rather than leaving them scattered across private notes.</p> <p>I call this blog <strong>Latent Thoughts</strong>, where I’ll be writing as I think. Most of what ends up here starts as a half-formed idea I don’t want to lose. I write it down and come back to it later, when it finally makes sense. Or maybe when I realize it didn’t :)</p> <p>I plan to share two kinds of posts here:</p> <ul> <li> <p>The first is <strong>research notes</strong>: what I’m currently interested in, opinions I want to commit to instead of leaving vague, explanations I write mostly to organize my own understanding of a topic, and the occasional review of a paper I thought was worth digesting properly.</p> </li> <li> <p>The second is <strong>meta-research</strong>: not the research itself, but how I learn to do it, how to actually get research done instead of just thinking about doing it, how to find and talk to the right people… DISCLAIMER: I am in no pisition to give advice to anyone. If reading some of these notes sparks a useful thought for yourself, I will be very happy ; but this is no advice or guide to become a great researcher.</p> </li> </ul> <p>The reason I bother to write any of this down instead of just thinking it through is because thinking is messy: it’s easy to lose a good idea, or rediscover the same one twice (and feel briefly proud both times), mostly because I never wrote the first one down. Putting it into words, especially on paper, is what makes an idea durable enough to build on instead of starting from scratch every time.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Why I'm writing a blog, and what to expect from it.]]></summary></entry></feed>